
/*
  
  Não coloquei o script para criação do banco "netflix" aki pq criei dentro do pgAdmin
  
*/

-- DROP TABLE Filmes CASCADE;

CREATE TABLE Filmes (
  Id 		    INTEGER NOT NULL GENERATED ALWAYS AS IDENTITY,
  Nome 		  VARCHAR(100)  NOT NULL,
  Ano		    INTEGER NOT NULL,
  Genero	  VARCHAR(25) NOT NULL,
  Descricao VARCHAR(300) ,
  Tipo		  VARCHAR(7),
  Imagem	  VARCHAR(250) NOT NULL,

PRIMARY KEY (id) );


-- DROP INDEX filmes_codigo;

CREATE INDEX filmes_codigo
  ON Filmes(Codigo);


-- exemplo para testes
INSERT INTO Filmes(Nome, Ano, Genero, Descricao, Tipo, imagem) 
  VALUES('Braking Bad', 2019, 'Drama', 'A Breaking Bad Movie é um filme norte-americano de drama lançado em 2019', "filme", 'mini4.jpg');

